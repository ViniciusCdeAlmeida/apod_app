import 'package:flutter/material.dart';

class ApodSavedScreen extends StatelessWidget {
  static const routeName = '/apod_saved_screen';

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('ApodSavedScreen')),
    );
  }
}
